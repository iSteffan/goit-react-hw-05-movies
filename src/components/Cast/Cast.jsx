// const { useParams } = require('react-router-dom');

const Cast = () => {
  // const { dogId } = useParams();

  // useEffect(() => {
  // HTTP запрос, если нужно
  // }, [])

  return <div>Cast</div>;
};

export default Cast;
